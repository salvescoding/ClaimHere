<div class="profile-card" style="position: fixed;">
  <% if current_user.first_name %>
  <h2><%= current_user.first_name %>'s profile</h2>
  <% else %>
  <h2>Profile</h2>
  <% end %>

  <div class="text-center">
     <% avatar_url = current_user.facebook_picture_url || current_user.photo || image_path("profile") %>
     <%= image_tag avatar_url, class: "avatar-large dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
   </div>

  <br>
  <br>
  <p><%= @user.email %></p>
  <p>Complaints submitted: <%= @user.complaints_submitted %></p>
  <p>Complaints answered: 1.790</p>
  <p>Complaints solved: 55%</p>
  <br>
  <br>

    <%= link_to "Edit my profile", edit_user_path(@user), class: "btn-claim" %>
<br>
<br>

</div>
